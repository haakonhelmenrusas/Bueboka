import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  notchContainer: {
    alignItems: 'center',
    marginBottom: 8,
  },
  notch: {
    width: 40,
    height: 4,
    backgroundColor: '#ccc',
    borderRadius: 2.5,
  },
});
