import { Layout } from "../../components";
import Form from "../../components/form/Form";

const FormPage = () => {
  return (
    <Layout>
      <Form />
    </Layout>
  );
};

export default FormPage;
